# ============================================================
# 暗黑破坏神4 游戏助手 - 依赖列表
# 分组安装见 README；下方为完整依赖。
# ============================================================

# ── 核心：GUI + 屏幕捕获 + 数据/搜索 ──────────────────────────
PyQt5>=5.15.0
mss==9.0.1
opencv-python==4.9.0.80
numpy==1.26.4
Pillow==10.2.0
requests==2.31.0
beautifulsoup4==4.12.3
keyboard==0.13.5
pystray==0.19.5          # 系统托盘图标
dxcam==0.0.5             # Windows 高性能屏幕捕获（优先于 mss）

# ── OCR 文字识别（config.py 默认 easyocr，中文识别准确率最高）──
easyocr==1.7.1           # 默认 OCR 引擎
# pytesseract==0.3.10    # 备选，另需安装 Tesseract-OCR 软件

# ── 语音：识别 (STT) + 播报 (TTS) ────────────────────────────
SpeechRecognition==3.10.1
pyttsx3==2.90            # 离线 TTS（回退）
edge-tts==6.1.12         # 默认 TTS（微软在线，音质好，需联网）
pygame==2.5.2            # 音频播放
pyaudiowpatch==0.2.12.7  # 音频输入/系统回录（优先）
sounddevice==0.4.6       # 音频输入回退方案
# openai-whisper         # 可选：高精度离线语音识别（需较大磁盘/GPU）

# ── 网站数据爬虫 ─────────────────────────────────────────────
selenium==4.18.1
webdriver-manager==4.0.1

# ── 测试（开发用，可选）──────────────────────────────────────
# pytest
